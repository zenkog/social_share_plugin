name: social_share_package
description: Social Share to Facebook and Instagram Flutter plugin.
version: 0.4.1+1
homepage: https://romatroskin.github.io/
repository: https://github.com/romatroskin/social_share_package.git
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.cygnati.social_share_package
        pluginClass: SocialSharePackage
      ios:
        pluginClass: SocialSharePackage
